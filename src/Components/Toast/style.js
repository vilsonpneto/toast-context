import styled from "styled-components";

export const List = styled.ul`
  position: absolute;
  z-index: 1000;
  top: 0;
  right: 0;

  display: flex;
  flex-direction: column;
  gap: 5px;
  padding: 3px;
`;
